# A class used to store the x and y coordinates of points

# La classe Point, qui permet de représenter chaque paire de coordonnées (x, y) de l'ensemble.
class Point:

	def __init__(self, x = None, y = None):
		self.x = x
		self.y = y